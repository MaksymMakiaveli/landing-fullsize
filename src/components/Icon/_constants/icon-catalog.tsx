import { ArrowDirectionIcon, LogoIcon, SuccessIcon } from '../_components';

export const iconCatalog = {
  logo: <LogoIcon />,
  'arrow-direction': <ArrowDirectionIcon />,
  success: <SuccessIcon />,
} as const;
