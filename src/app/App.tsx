import { HeaderSection, HeroSection, ProductSection } from '@components';

import './styles.scss';

export function App() {
  return (
    <main>
      <HeaderSection />
      <HeroSection />
      <ProductSection />
    </main>
  );
}
